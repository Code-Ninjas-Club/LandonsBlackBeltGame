using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    int difficulty = 0;

    string character = "Not snowey";


    // Start is called before the first frame update
    void Start()
    {
        switch (character)
        {
            case "Windows xp":

                break;
              
        }
    switch (difficulty)
    {
            case 1:

            case 2:

            case 3:

            default:
                break;
    }
}

    // Update is called once per frame
    void Update()
    {
        
    }
}
